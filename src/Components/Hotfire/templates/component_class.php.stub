<?php

declare(strict_types=1);

namespace {{namespace}};

use Components\Hotfire\Component;
use Components\Hotfire\Exception\InvalidStateException;

/**
 * Reactive {{class}} component (generated by make:hotfire).
 * 
 * This component follows Hotfire best practices:
 * - Type-safe public properties for state management
 * - Automatic state serialization via Snapshot
 * - CSRF protection through signed snapshots
 * - Server-side rendering with Alpine.js integration
 * 
 * @final To prevent inheritance and ensure predictable behavior
 */
final class {{class}} extends Component
{
    /** Rendered by Hotfire from this exact template (Hotfire view prefix). */
    protected string $view = '{{view}}';

{{propsAndSave}}
    /**
     * Validates the component state before rendering.
     * 
     * Override this method to add custom validation logic.
     * Throw InvalidStateException for validation failures.
     * 
     * @throws InvalidStateException When state is invalid
     * @return void
     */
    protected function validateState(): void
    {
        // Example validation:
        // if (empty($this->title)) {
        //     throw new InvalidStateException('Title is required');
        // }
    }

    /**
     * Returns the component state for snapshot serialization.
     * 
     * Override this method to control what gets serialized.
     * Default behavior includes all public properties.
     * 
     * @return array<string, mixed>
     */
    public function state(): array
    {
        return parent::state();
    }

    /**
     * Lifecycle hook called before rendering.
     * 
     * Use this for initialization, data loading, or preprocessing.
     * 
     * @return void
     */
    protected function beforeRender(): void
    {
        // Example: Load data from database
        // $this->posts = model(PostModel::class)->findAll();
    }

    /**
     * Lifecycle hook called after rendering.
     * 
     * Use this for cleanup or post-render processing.
     * 
     * @return void
     */
    protected function afterRender(): void
    {
        // Example: Log component usage
        // log_message('info', 'Component {class} rendered');
    }
}
